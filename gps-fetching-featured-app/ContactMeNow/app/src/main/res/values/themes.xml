<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ContactMe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="ToolbarTitleTextAppearance">
        <item name="android:textSize">18sp</item>  <!-- Adjust text size as needed -->
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>  <!-- Optional: make text bold -->
    </style>

    <style name="Theme.ContactMe" parent="Base.Theme.ContactMe" />
</resources>